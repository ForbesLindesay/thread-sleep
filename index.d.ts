/**
 * Attempt to wait the requested number of milliseconds.
 *
 * Returns the number of milliseconds actually waited.
 */
declare function sleep(milliseconds: number): number;

export default sleep;
